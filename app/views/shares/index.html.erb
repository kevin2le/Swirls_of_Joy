<div class="swirls">
    <h1>Share my Cone</h1>
        <h3>Feel free to post your opinions, pictures and<br> where you found some awesome ice cream</h3>
</div>
<div class= ".text-center">    
    <div class="font">
        <dl class= "profile">
            <% @shares.each do |share| %>
            <div class="bckclr">
                <dl><%= image_tag share.image_file_name, size: "370x300" %></dl>
                    <dd><%= share.nickname %> </dd>
                    <dd><%= share.location %></dd>
                    <dd><%= share.content %></dd>
            </div>
            <% end %>
        </dl>
    </div>
</div>
<h3>POST!</h3>
<%= form_for @share do |share| %>
   <div class="field">
        <%= share.label :nickname %>
        <%= share.text_field :nickname %>
    </div>
    <div class="field">
        <%= share.label :location %>&nbsp&nbsp
        <%= share.text_field :location %>
    </div>
    <div class="field">
        <%= share.label :content %>&nbsp&nbsp&nbsp
        <%= share.text_field :content %>
    </div>
    <div class="field">
        <%= share.file_field :image_file_name %></p>
    </div>
    <div class="actions"><%= share.submit %></div>
    <% end %>


   
   